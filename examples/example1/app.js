var pillars = require('../../pillars.js').global().configure({
	uploadsDirectory : './uploads',
	tempDirectory : './temp2',
	requestIds : false,
	templatesCache : false,

});

pillars.uploadsDirectory; 

pillars.timeout = 10* 2000;

PILLARS.errorsTemplate = "";
PILLARS.package.version;
PILLARS.path("./templates/crud.jade");




var app = new App({
	languages:
	databse :
});
app.languages = ['es','en'];



app.database = 'primera';

app.on('database',function(){
	app.database.getCollection('sessions');
});

app.add(new Pillar({id:}));


app.start(3000);

var statusPillar = require('./statusPillar.js');

app.add(statusPillar);


host: 
.*\.asd.(com|es)

pillar.path = '/(uno|dos)/';


var beam = new Beam({path:'(/:name)/(:action)',method:['get','post'],cors:'www.asd.asd'},function(gw){
	beam2.launch(gw);
	beam2.handler(gw); 
});
beam.config.path = 
beam.path = 

beam.handler = function(gw){};
beam.middleware = [function(gw){},function(gw){}]
beam.params = ['name','action'];